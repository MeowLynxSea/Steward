# Memory

Legacy curated notes and optional workspace context.

This file is not the runtime source of truth for long-term memory.
Durable facts, preferences, agreements, and self-model details belong in the native graph memory system.

If you keep anything here, keep it concise and treat it as user-authored workspace context, not something the agent should append to during normal conversation.
